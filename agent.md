# Agent entrypoint

The canonical coding-agent instructions are in [`AGENTS.md`](./AGENTS.md).

An implementation agent must read `AGENTS.md` in full before changing code, then follow [`TASKS.md`](./TASKS.md) and the linked specifications under `docs/`.

Do not maintain separate rules in this lowercase compatibility file; `AGENTS.md` is the source of truth.
